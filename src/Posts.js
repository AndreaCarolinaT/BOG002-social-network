//Función para pintar posts 
/* export function showPosts() { 
    const postCard = document.getElementById("timelinePosts");
      const info = doc.data();
      postCard.innerHTML += `<div class="contentPosts">
      <h3>${info.title}</h3>
      <p>${info.description}</p>
      </div>`
} 
 */

